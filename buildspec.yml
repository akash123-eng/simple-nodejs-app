version: 0.2

phases:
  install:
    commands:
      - echo "Installing dependencies..."
    runtime-versions:
      nodejs: latest

  pre_build:
    commands:
      - echo "Pre-build phase"

  build:
    commands:
      - echo "Building application..."
      - grep -Fq "john" index.html

  post_build:
    commands:
      - echo "Build completed!"
